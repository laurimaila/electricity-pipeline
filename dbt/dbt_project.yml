name: "electricity_pipeline_dbt"
version: "1.0.0"
config-version: 2

profile: "electricity_pipeline"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  vat_multiplier: 1.255

models:
  electricity_pipeline_dbt:
    marts:
      +materialized: materialized_view
